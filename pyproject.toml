[project]
name = "applypilot"
version = "0.5.0"
description = "AI-powered end-to-end job application pipeline (multi-resume, strict tailoring, tracking)"
readme = "README.md"
license = "AGPL-3.0-only"
requires-python = ">=3.11"
authors = [{name = "Pickle-Pixel"}, {name = "lohithveerepalli"}]
keywords = [
    "job-search", "automation", "ai", "resume", "apply", "job-application",
    "sre", "platform-engineer", "network-engineer", "infrastructure",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business",
]

dependencies = [
    "typer>=0.9.0",
    "rich>=13.0",
    "httpx>=0.24",
    "beautifulsoup4>=4.12",
    "playwright>=1.40",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
    "pandas>=2.0",
    ]

[project.optional-dependencies]
dev = ["pytest>=7.0", "ruff>=0.1"]

[project.scripts]
applypilot = "applypilot.cli:app"

[project.urls]
Homepage = "https://github.com/lohithveerepalli/ApplyPilot"
Repository = "https://github.com/lohithveerepalli/ApplyPilot"
Issues = "https://github.com/lohithveerepalli/ApplyPilot/issues"
Upstream = "https://github.com/Pickle-Pixel/ApplyPilot"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/applypilot"]

[tool.hatch.build]
artifacts = ["src/applypilot/config/*.yaml"]

[tool.ruff]
target-version = "py311"
line-length = 120
